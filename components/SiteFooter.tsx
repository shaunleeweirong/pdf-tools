export function SiteFooter() {
  return (
    <footer className="mt-24 border-t border-white/10">
      <div className="mx-auto flex max-w-6xl items-center gap-2 px-4 py-10 font-mono text-xs uppercase tracking-[0.14em] text-muted-foreground">
        <span className="inline-block size-1.5 bg-brand" aria-hidden />
        All tools run in your browser — your files never leave your device.
      </div>
    </footer>
  )
}
